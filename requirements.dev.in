--constraint requirements.prod.txt

# Additional dev requirements
# To generate a requirements file that includes both prod and dev requirements, run:
# pip-compile --generate-hashes --output-file=requirements.dev.txt requirements.dev.in

black
check-manifest
coverage
flake8
flake8-builtins
flake8-implicit-str-concat
flake8-no-pep420
hypothesis
isort
pip-tools
pre-commit
pytest

# analysis code tests
#matplotlib==3.1.3
#numpy==1.18.1
#pandas==1.0.1

# functional tests requirements
opensafely  # because jobrunner cannot currently be installed in --require-hashes
